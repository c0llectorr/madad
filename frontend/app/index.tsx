// Root index — the app entry point is handled entirely by _layout.tsx,
// which mounts AppNavigator (React Navigation). AppNavigator owns all
// routing logic (onboarding flag + auth state). Nothing to render here.
export default function Index() {
	return null
}
